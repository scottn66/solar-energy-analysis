# Data files (too large for git)
*.zip
*.parquet

# Large data CSVs (downloaded at runtime)
TTS_LBNL_*.csv
solar_master_feature_table.csv
enriched_sites.csv
data/tts_raw.csv
data/tts_raw.zip
data/tts_cleaned.csv
etl/cleaning.log

# Keep sample data files
# !sample_site.csv is small and committed

# Jupyter checkpoints
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Environment
.env
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# API response caches (generated at runtime)
*.sqlite
site_reports/
solar_report.html
node_modules
package*.json

# Generated HTML reports (use --output to write them; not for git)
report_*.html

# Synthetic portfolio outputs (regenerable via build_portfolio.py)
portfolio_sites.csv
portfolio_sites.json
portfolio_for_dashboard.json
portfolio_for_dashboard.min.json
portfolio_dashboard.html

# Generated heatmap HTML (regenerable via build_heatmap.py)
heatmap_ca.html
heatmap_norcal.html
heatmap_or.html
heatmap_central_or.html

# Local editor / agent state
.claude/

# GitHub Pages deploy clone (nested git repo from earlier deploy attempt)
_deploy/

# Word-doc copies of generated docs (the .md is the source of truth)
docs/*.docx

# DuckDB warehouse — built by ETL, can be regenerated.
# If you want teammates to pull a pre-built snapshot, comment these lines out
# and commit the .duckdb file directly.
data/warehouse/
*.duckdb
*.duckdb.wal
